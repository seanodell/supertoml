[test]
name = "Environment variable error handling"
description = "Test that env() function properly errors when environment variable doesn't exist"
table = "error_config"
expected_error = "Environment variable.*not found"

[error_config]
# This should cause an error because the environment variable doesn't exist
missing_var = "{{ env('DEFINITELY_NONEXISTENT_VAR_123456') }}"
